name: CI

on:
  push:
  pull_request:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  quality:
    name: Windows quality gate
    runs-on: windows-latest
    timeout-minutes: 30

    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Set up Node.js
        uses: actions/setup-node@v7
        with:
          node-version: 24.20.0
          cache: npm

      - name: Select pinned Rust toolchain
        shell: pwsh
        run: rustup show active-toolchain

      - name: Install JavaScript dependencies
        run: npm ci

      - name: Validate frontend
        run: npm run check

      - name: Validate performance budgets
        run: npm run test:performance

      - name: Check Rust formatting
        working-directory: src-tauri
        run: cargo fmt --all -- --check

      - name: Check native targets
        working-directory: src-tauri
        run: cargo check --locked --all-targets

      - name: Test SQLite foundation
        working-directory: src-tauri
        run: cargo test --locked --all-targets

      - name: Lint Rust
        working-directory: src-tauri
        run: cargo clippy --locked --all-targets -- -D warnings

      - name: Run minimum ProjectFlow journey
        # Native SQLite/portability coverage already ran above. Keeping the
        # application journey separate avoids reopening the same temporary
        # backup files twice in one Windows runner job.
        run: npm run test:e2e:application

      - name: Build Windows installer
        # Release builds are signed outside this read-only quality gate. The CI
        # still validates the complete NSIS bundle without requiring secrets.
        run: npm run tauri:build:installer:ci
